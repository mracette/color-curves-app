{
  "name": "color-curves-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w packages/app",
    "build": "npm run build -w packages/app",
    "test": "npm run test -w packages/color-curves && npm run test -w packages/app"
  }
}
